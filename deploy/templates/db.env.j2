# Managed by volunteerdb deploy/deploy.py - do not edit by hand.
# Consumed by the postgres:17 container; applied only at first initdb of an
# empty volume. Rotating the password later requires a manual ALTER ROLE.
POSTGRES_USER={{ db_user }}
POSTGRES_PASSWORD={{ db_password }}
POSTGRES_DB={{ db_name }}
